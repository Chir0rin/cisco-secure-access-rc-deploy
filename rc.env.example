# Copy to rc.env on the RC VM. Never commit rc.env with real keys.
# cp rc.env.example rc.env && chmod 600 rc.env

RC_NAME=himorish-cslab-rc01
RC_PROVISIONING_KEY='PASTE_FROM_SECURE_ACCESS_DASHBOARD'
RC_YES=1

# CS lab proxy (SharePoint: tyoidc5-dmz-wsa-* when proxy.esl is slow)
RC_HTTP_PROXY=http://tyoidc5-dmz-wsa-1.cisco.com:80
# RC_HTTP_PROXY=http://tyoidc5-dmz-wsa-2.cisco.com:80
# RC_HTTP_PROXY=http://proxy-ams-1.cisco.com:8080
